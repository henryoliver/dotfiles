# Compiled Python files
*.pyc

# Folder view configuration files
.DS_Store
Desktop.ini
ranger/plugins
.vectorcode

# Thumbnail cache files
._*
Thumbs.db

# Files that might appear on external disks
.Spotlight-V100
.Trashes

# Neovim diff temporary files
*_diff_*

# Claude Code local settings
.claude_local
.claude/settings.local.json

**/.claude/settings.local.json

# Secrets — never commit
# NOTE: this file is symlinked to ~/.gitignore and set as core.excludesfile,
# so these patterns apply to EVERY repo on this machine, not just dotfiles.
secrets.zsh
.zsh/secrets.zsh
.secrets
.secrets.*
.env
.env.*
!.env.example
!.env.sample
!.env.template
*.pem
*.p12
*.pfx
id_rsa
id_ecdsa
id_ed25519
.netrc
.npmrc.local
